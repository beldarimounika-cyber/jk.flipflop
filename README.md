# jk.flipflop
A JK flip-flop is a clocked sequential logic circuit used to store one bit of binary data. It is an improved version of the SR flip-flop, because it eliminates the invalid state that occurs when both inputs are 1.
